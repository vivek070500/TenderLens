You are an expert at reading procurement and tender documents. Scope, sector, and buyer vary, so the **set of eligibility criteria is never fixed** — extract **only** what **this** document actually requires.

Your task: list every **distinct** eligibility or qualification obligation stated between <<<TENDER>>> and <<<END_TENDER>>>.

VARIETY ACROSS TENDERS:

- **No checklist.** Do **not** assume EMD, turnover, “similar works”, GST, or any item unless equivalent wording appears in <<<TENDER>>>.

- **Do not invent** boilerplate typical of “generic government tenders”.

GROUND-TRUTH:

- Each criterion must map to wording or structure inside <<<TENDER>>>.

- Output **as many or as few** criteria as truly distinct obligations exist — dozens or a handful both happen.

**DEDUPLICATION (critical for e‑procurement / CPWD‑style portals):**

- Procurement portals often show the **same mandatory document list twice** under different headings (e.g. “Mandatory Documents” and “Eligibility Documents Details”). If rows describe the **same** proof or certificate, emit **one** criterion only.

- Consolidate wording: one description that covers the requirement; combine “source_section” mentions if helpful (e.g. “Mandatory Documents; Eligibility Documents Details”).

- If two rows differ only by column fillers (“Mandatory”, serial numbers), they are duplicates.

**WHAT NOT TO COUNT AS ELIGIBILITY CRITERIA:**

- **Pure bid structure / covers / envelopes**: tables that only name Technical Bid / Financial Bid / “Single Bid”, cover labels, upload slots, or file names — unless they impose a **new substantive** obligation (beyond “submit bids in prescribed covers”) already covered elsewhere.

- **Navigation metadata**: tenders listed for download without adding new bidder obligations.

Treat each remaining **distinct** obligation as its own criterion when the text separates them (clauses, bullet lists that add new requirements).


CATEGORY ASSIGNMENT:

- "financial"  → turnover, net worth, EMD, bonds, deposits, capital, monetary tests

- "experience" → past projects / completion / similar works the bidder must prove

- "technical"  → methods, equipment, **personnel qualifications** (staff roles go here — not past-company projects)

- "compliance" → registrations, licences, certificates, declarations, enlistment/GST/ISO-style proofs

IMPORTANT: Personnel experience (“engineer with N years…”) → **technical**; company past works → **experience**.


THRESHOLD EXTRACTION:

When there is a number or bound, use "threshold" in these patterns:

- Currency (INR rupees numerals): ">= <amount> INR" …

- Time: ">= <n> years" or ">= <n> months"

- Count: ">= <n> projects" (when count is explicit)

- Otherwise: short condition phrase from the tender (e.g. "valid enlistment certificate")

INDIAN RUPEE NUMERALS:

- 1 Lakh = 100000; 1 Crore = 10000000 (e.g. 2,00,00,000 = 20000000 = 2 Crore)


Each criterion object MUST have:

- "criterion_id": "C-001", … in final order after merging duplicates

- "description": clear requirement; preserve currency/count phrases useful for downstream parsing where present

- "category": "financial", "experience", "compliance", or "technical"

- "mandatory": true unless clearly desirable/optional in the tender

- "threshold": numeric/checkable threshold or ""

- "expected_evidence": what the bidder submits for **this** requirement

- "source_section": heading, table title, clause, or page reference (best effort)


Return ONLY valid JSON: {"criteria": [ ... ]} with no markdown fences.



<<<TENDER>>>

{tender_text}

<<<END_TENDER>>>

